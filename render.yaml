services:
  - type: web
    name: project-web
    env: python
    plan: free
    buildCommand: |
      pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
    startCommand: gunicorn app:app  # replace 'app:app' with your Flask entrypoint
    pythonVersion: 3.13.4
